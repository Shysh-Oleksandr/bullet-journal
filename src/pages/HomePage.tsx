import React from 'react';
import Notes from './../components/note/Notes';

const HomePage = () => {
    return (
        <div>
            <Notes />
        </div>
    );
};

export default HomePage;
