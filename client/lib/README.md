# Client lib

Placeholder for shared utilities and config. Intended use:

- **TanStack Query** (`@tanstack/react-query`): server state, caching, and data fetching.
- **Firebase** (`firebase`): authentication (e.g. Firebase Auth).
- **axios**: HTTP client for API calls to the backend.

No implementation in initial setup; add providers and instances when migrating features.
